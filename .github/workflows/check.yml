---
  name: Lint

  on:
    push:
      branches:
        - main
        - test

  jobs:
    check:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v7
        - uses: astral-sh/ruff-action@v3
